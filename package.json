{
	"name": "arbetslag-monorepo",
	"private": true,
	"packageManager": "pnpm@10.28.2",
	"scripts": {
		"build": "pnpm -r --workspace-concurrency=1 build",
		"type-check": "pnpm -r type-check",
		"demo:telegram": "pnpm --filter arbetslag run build && pnpm --filter telegram-bot run start"
	}
}
